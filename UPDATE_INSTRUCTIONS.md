# Обновление проекта - Полный фронтенд + миграции с данными

## Что было добавлено:

### ✅ Frontend (Next.js + React)
- **app/page.tsx** - главная страница со списком поставщиков
- **app/layout.tsx** - layout с metadata
- **app/globals.css** - Tailwind CSS стили
- **tailwind.config.js** - конфигурация Tailwind
- **postcss.config.js** - PostCSS конфиг
- **package.json** - добавлен `lucide-react` для иконок

### ✅ Backend
- **audit_log.py** - исправлен конфликт `metadata` → `extra_metadata`
- **seed_001_initial_seed.py** - миграция с тестовыми данными:
  - 2 пользователя (admin, manager)
  - 4 поставщика (включая blacklist)

## Быстрый запуск:

```bash
cd /root/dai

# 1. Пересобрать контейнеры (только backend изменился!)
docker compose build api celery_worker_email celery_worker_parsing celery_worker_search celery_beat

# 2. Пересобрать frontend
docker compose build frontend

# 3. Перезапустить всё
docker compose down
docker compose up -d

# 4. Подождать 30 секунд
sleep 30

# 5. Выполнить миграции с тестовыми данными
docker compose exec api alembic upgrade head

# 6. Проверить статус
docker compose ps

# 7. Открыть в браузере
echo "Frontend: http://localhost:3000"
echo "API Docs: http://localhost:8000/docs"
```

## Тестовые данные в БД:

### Пользователи:
```
Email: admin@company.ru
Password: admin123
Role: Admin

Email: manager@company.ru  
Password: manager123
Role: Manager
```

### Поставщики:
1. **СтройКомплект** (ИНН: 7701234567)
   - Рейтинг: 4.3
   - Теги: #Цемент #Knauf #Сухие смеси #Бетон

2. **ЭлектроМир** (ИНН: 7709876543)
   - Рейтинг: 4.3
   - Теги: #ABB #Schneider #Кабель #Розетки #Электрика

3. **Рога и Копыта** (ИНН: 7700000000) ⚠️ BLACKLIST
   - Рейтинг: 1.0
   - Причина: Мошенничество
   - Теги: #Воздух

4. **ООО** (ИНН: 7724422835)
   - Рейтинг: 0.0
   - Теги: #Заявка на #Градация

## Функции фронтенда:

✅ Список поставщиков с карточками
✅ Поиск по названию/ИНН/тегам
✅ Рейтинги со звёздочками
✅ Теги (кликабельные)
✅ Blacklist индикатор (красная метка)
✅ Боковое меню с навигацией
✅ Роли пользователей (Admin/Manager)
✅ Responsive дизайн

## API Endpoints (готовые):

```bash
# Получить список поставщиков
GET http://localhost:8000/api/suppliers

# Поиск поставщика
GET http://localhost:8000/api/search?q=Knauf

# Получить одного поставщика
GET http://localhost:8000/api/suppliers/{id}
```

## Если что-то не работает:

```bash
# Логи API
docker logs db_ai_api --tail 50

# Логи Frontend
docker logs db_ai_frontend --tail 50

# Проверить БД
docker compose exec postgres_master psql -U dbai_user -d database_ai -c "SELECT company_name, inn, rating FROM suppliers;"

# Перезапустить всё с нуля
docker compose down -v
docker compose up -d
docker compose exec api alembic upgrade head
```

## Следующие шаги:

1. ✅ Фронтенд работает
2. ✅ API работает
3. ✅ Данные в БД
4. 🔄 Добавить авторизацию (JWT)
5. 🔄 Добавить страницу деталей поставщика
6. 🔄 Добавить форму создания/редактирования
7. 🔄 Добавить загрузку прайсов
8. 🔄 Добавить AI парсинг

## Порты:

- Frontend: http://localhost:3000
- API: http://localhost:8000
- API Docs: http://localhost:8000/docs
- PostgreSQL: localhost:5432
- MinIO Console: http://localhost:9001
