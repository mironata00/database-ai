# 🚀 Database AI - Быстрый старт

## Система управления базой поставщиков с AI-парсингом прайс-листов

---

## 📋 Содержание

1. [Требования](#требования)
2. [Быстрая установка](#быстрая-установка)
3. [Настройка портов](#настройка-портов)
4. [Первый запуск](#первый-запуск)
5. [Доступ к системе](#доступ-к-системе)
6. [Тестовые данные](#тестовые-данные)
7. [Устранение проблем](#устранение-проблем)

---

## 💻 Требования

### Минимальные:
- **RAM**: 4GB (рекомендуется 8GB)
- **Диск**: 20GB свободного места
- **CPU**: 2 ядра (рекомендуется 4)
- **ОС**: Ubuntu 20.04+ / Debian 11+

### Программное обеспечение:
- **Docker**: 24.0+
- **Docker Compose**: 2.20+

---

## ⚡ Быстрая установка

### 1. Скачать проект

```bash
# Распаковать архив
cd /root
tar -xzf database-ai.tar.gz
cd database-ai
```

### 2. Настроить окружение

```bash
# Скопировать пример конфигурации
cp .env.example .env

# Отредактировать под свои нужды (опционально)
nano .env
```

**Важные параметры в `.env`:**
```bash
# Порты (измените если заняты)
API_PORT=8000
FRONTEND_PORT=3000

# Безопасность (ОБЯЗАТЕЛЬНО измените в production!)
SECRET_KEY=your-secret-key-min-32-chars
POSTGRES_PASSWORD=your_secure_password
REDIS_PASSWORD=your_redis_password
```

### 3. Запустить проект

```bash
# Дать права на выполнение
chmod +x quick-start.sh

# Запустить автоматическую установку
./quick-start.sh
```

**Скрипт автоматически:**
- ✅ Проверит наличие Docker
- ✅ Создаст `.env` файл
- ✅ Соберёт все образы
- ✅ Запустит сервисы
- ✅ Выполнит миграции БД
- ✅ Создаст тестовые данные

---

## 🔌 Настройка портов

### Для локальной разработки (localhost)

Используйте стандартные порты из `.env`:
```bash
API_PORT=8000
FRONTEND_PORT=3000
```

### Для удалённого сервера (VPS/хостинг)

#### Вариант 1: Через Nginx (рекомендуется)

```bash
# Nginx автоматически пробросит порт 80
# Frontend: http://ваш-домен.ru
# API: http://ваш-домен.ru/api
```

#### Вариант 2: Прямой доступ к портам

Если у вас хостинг с пробросом портов (например, Myjino):

1. **В панели хостинга** настройте проброс:
   - Внешний порт `49349` → Внутренний `3000` (Frontend)
   - Внешний порт `49351` → Внутренний `8000` (API)

2. **В `.env`** укажите внешние порты:
```bash
FRONTEND_PORT=49349
API_PORT=49351
```

3. **Перезапустите**:
```bash
docker compose down
docker compose up -d
```

---

## 🎯 Первый запуск

### Проверить статус

```bash
cd /root/database-ai

# Все контейнеры должны быть healthy
docker compose ps
```

**Ожидаемый вывод:**
```
NAME                    STATUS
db_ai_api               Up (healthy)
db_ai_frontend          Up
db_ai_postgres_master   Up (healthy)
db_ai_redis_master      Up (healthy)
db_ai_elasticsearch     Up (healthy)
db_ai_minio             Up (healthy)
db_ai_celery_*          Up
db_ai_nginx             Up
```

### Создать базу данных

```bash
# Выполнить миграции
docker compose exec api alembic upgrade head

# Создать тестовые данные
docker compose exec -T postgres_master psql -U postgres -d database_ai < /root/database-ai/seed.sql
```

---

## 🌐 Доступ к системе

### Локальный доступ (localhost)

- **Frontend**: http://localhost:3000
- **API Docs**: http://localhost:8000/docs
- **MinIO Console**: http://localhost:9001

### Удалённый доступ (VPS)

#### Через Nginx (порт 80):
- **Frontend**: http://ваш-домен.ru
- **API Docs**: http://ваш-домен.ru:8000/docs

#### Через прямые порты:
- **Frontend**: http://ваш-домен.ru:49349
- **API Docs**: http://ваш-домен.ru:49351/docs

---

## 🧪 Тестовые данные

### Пользователи

```
Email: admin@company.ru
Пароль: admin123
Роль: Администратор

Email: manager@company.ru
Пароль: manager123
Роль: Менеджер
```

### Поставщики

После создания БД в системе будет 4 тестовых поставщика:

1. **СтройКомплект** ⭐ 4.3
   - ИНН: 7701234567
   - Теги: #Цемент #Knauf #Сухие смеси #Бетон

2. **ЭлектроМир** ⭐ 4.3
   - ИНН: 7709876543
   - Теги: #ABB #Schneider #Кабель #Розетки

3. **Рога и Копыта** ⭐ 1.0 🚫 BLACKLIST
   - ИНН: 7700000000
   - Причина: Мошенничество

4. **ООО** ⭐ 0.0
   - ИНН: 7724422835
   - Теги: #Заявка #Градация

---

## 🔧 Устранение проблем

### Порты заняты

```bash
# Проверить что занимает порт
sudo netstat -tulpn | grep :3000

# Изменить порты в .env
nano .env
# Изменить FRONTEND_PORT и API_PORT

# Перезапустить
docker compose down
docker compose up -d
```

### Не хватает памяти

```bash
# Уменьшить потребление Elasticsearch
echo "ES_HEAP_SIZE=512m" >> .env

# Перезапустить
docker compose down
docker compose up -d
```

### Контейнеры не запускаются

```bash
# Посмотреть логи
docker compose logs api
docker compose logs frontend
docker compose logs postgres_master

# Пересоздать с нуля
docker compose down -v
docker compose up -d
```

### API не отвечает

```bash
# Проверить здоровье API
curl http://localhost:8000/health

# Если не отвечает - перезапустить
docker compose restart api

# Логи
docker logs db_ai_api --tail 50
```

### Frontend не загружается

```bash
# Проверить статус
docker compose ps | grep frontend

# Логи
docker logs db_ai_frontend --tail 30

# Пересобрать
docker compose build frontend
docker compose up -d frontend
```

### Ошибки миграций БД

```bash
# Сбросить миграции
docker compose exec api alembic stamp base

# Применить заново
docker compose exec api alembic upgrade head

# Если не помогло - пересоздать БД
docker compose down -v
docker compose up -d
docker compose exec -T postgres_master psql -U postgres -d database_ai < /root/database-ai/seed.sql
```

---

## 📚 Дополнительные команды

### Управление контейнерами

```bash
# Запустить всё
docker compose up -d

# Остановить всё
docker compose down

# Остановить и удалить данные
docker compose down -v

# Перезапустить сервис
docker compose restart api

# Посмотреть логи
docker compose logs -f api

# Выполнить команду внутри контейнера
docker compose exec api bash
```

### Резервное копирование

```bash
# Бэкап PostgreSQL
docker compose exec postgres_master pg_dump -U postgres database_ai > backup_$(date +%Y%m%d).sql

# Восстановление
docker compose exec -T postgres_master psql -U postgres -d database_ai < backup_20241226.sql
```

### Обновление проекта

```bash
cd /root/database-ai

# Остановить
docker compose down

# Обновить код
git pull

# Пересобрать
docker compose build

# Запустить
docker compose up -d

# Миграции
docker compose exec api alembic upgrade head
```

---

## 🎓 Следующие шаги

1. **Настроить email** для получения прайсов (см. `.env` секция EMAIL)
2. **Загрузить прайс-листы** через интерфейс
3. **Настроить AI-парсинг** (если нужно)
4. **Настроить SSL** для production
5. **Настроить backup** (рекомендуется)

---

## 📞 Поддержка

- **Документация**: `/docs` в проекте
- **API Docs**: http://localhost:8000/docs
- **Issues**: GitHub репозиторий

---

## ⚠️ Важно для Production

Перед развёртыванием в production **ОБЯЗАТЕЛЬНО**:

1. ✅ Измените все пароли в `.env`
2. ✅ Настройте SSL (HTTPS)
3. ✅ Настройте backup
4. ✅ Ограничьте доступ к портам
5. ✅ Настройте мониторинг
6. ✅ Отключите DEBUG режим

```bash
# В .env
APP_ENV=production
DEBUG=false
SECRET_KEY=<сгенерируйте_новый_ключ>
POSTGRES_PASSWORD=<надёжный_пароль>
REDIS_PASSWORD=<надёжный_пароль>
```

---

**Готово! Система запущена и готова к работе.** 🎉
